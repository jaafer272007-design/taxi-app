import 'package:flutter/material.dart';
import 'package:shared/shared.dart';

/// Run with `flutter run` from `packages/shared/example` to see the whole
/// Design System — every token and base widget, in light and dark, RTL.
void main() => runApp(const ThemePreviewApp());
